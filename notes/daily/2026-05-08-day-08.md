# Day 08 — 2026-05-08

## Was gemacht

- uv als Package Manager installiert und verifiziert
- Python 3.12 für das Projekt gepinnt via `.python-version`
- `pyproject.toml` mit src-Layout, hatchling Build-Backend und zwei
  Dependency-Groups (dev, test) erstellt
- `src/aect/__init__.py` mit `__version__ = "0.1.0"` angelegt
- `tests/__init__.py` angelegt
- `uv sync --group dev --group test` ausgeführt — .venv erstellt
- `.gitignore` um `.venv/` und Cache-Verzeichnisse ergänzt
- Import-Test erfolgreich: `import aect; print(aect.__version__)` → `0.1.0`
- Git-Commit gesetzt

## Was ich heute noch nicht verstanden habe

- Den Unterschied zwischen uv, pip, venv und pyproject.toml im Zusammenspiel
- Warum src-Layout statt direktes Package im Root
- Was pyproject.toml konkret macht vs. was früher setup.py gemacht hat
- Was dependency-groups bringt vs. einfach alles in requirements.txt

## Sichtbarer Output

- `pyproject.toml` (vollständig)
- `.python-version` (3.12)
- `src/aect/__init__.py`
- `tests/__init__.py`
- `.venv/` (uv-verwaltete virtuelle Umgebung)
- Git-Commit: `feat: init uv project with src-layout and Python 3.12`

## Morgen

Tag 9: ruff + mypy strict konfigurieren, alle Lint- und Type-Fehler beheben.