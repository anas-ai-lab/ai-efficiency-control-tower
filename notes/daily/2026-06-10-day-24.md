# Tag 24 — API-Key-Auth, Exception-Handler, GET /cases

**Datum:** 2026-06-10
**Phase:** B — API-Layer & Persistenz
**Commits:** feat(api): API-Key-Auth, globaler Exception-Handler, GET /cases

## Was gebaut wurde

- `src/aect/adapters/api/settings.py` — pydantic-settings, liest AECT_API_KEY
  aus Env/.env; extra="ignore" wegen PYTHONPATH in .env
- `src/aect/adapters/api/dependencies.py` — require_api_key (APIKeyHeader,
  auto_error=False), get_settings ohne lru_cache fuer Test-Overrides
- `src/aect/adapters/api/routes/cases.py` — GET /cases, auth-geschuetzt,
  CaseSummary-Response-Schema
- `src/aect/adapters/api/app.py` — globaler Exception-Handler (500 +
  request_id, kein Stack-Trace an Client)
- `tests/adapters/api/test_auth.py` — 11 Tests fuer Auth + Exception-Handler
- `.env.example` committed

## Coverage-Delta

dependencies.py: 0% → 97% (fehlende Zeile: get_triage_service uncovered path)
Gesamt: 97% → 99%
175 passed (war: 164)

## Probleme und Loesungen

**venv-Korruption (§6.2/§6.3):** ModuleNotFoundError trotz gruener pytest beim
Sanity-Import. Fix: rm -rf .venv && uv sync.

**pydantic-settings liest ganzes .env:** PYTHONPATH=src als unbekanntes Feld →
ValidationError. Fix: extra="ignore" in model_config.

**ruff B008:** False Positive fuer Depends() in Funktionssignaturen (FastAPI-
Standardmuster). Fix: # noqa: B008 an betroffenen Zeilen.

**Exception-Handler-Tests failed:** Handler lief korrekt (Log zeigte ERROR),
aber ASGITransport propagiert Starlette's Re-raise standardmaessig.
Fix: raise_app_exceptions=False in den zwei betroffenen Tests.

## Offen / Naechster Tag

Tag 25: Request/Response-Schemas + POST /triage (Kernendpoint).
Danach: Idempotency Keys, structlog, SQLite → Phase-B-Gate-Kandidat.
