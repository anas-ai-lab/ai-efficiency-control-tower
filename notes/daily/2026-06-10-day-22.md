# Tag 22 — 2026-06-10

## Was heute gebaut wurde

- Verzeichnisstruktur Phase B angelegt: `src/aect/application/`, `src/aect/adapters/`
- 3 Port-Protokolle: `ClockPort`, `IdGeneratorPort`, `RepositoryPort`
- 3 In-Memory-Adapter: `SystemClock`, `UUIDGenerator`, `InMemoryRepository`
- Application Models: `SubmittedCase` (DTOs verbinden Input + TriageResult + Metadaten)
- `TriageService`: orchestriert ID-Generierung, Zeitstempel, Domain-Evaluate, Persistenz
- `tests/conftest.py` bereinigt (doppeltes pytest_configure entfernt) + neue Fixtures
- Tests: `test_repository.py`, `test_service.py` — alle grün
- ADR-004: Hexagonal Architecture entschieden und dokumentiert

## Probleme und Fixes

- Schritt 7 (conftest): doppelter Header durch Einfügen statt Ersetzen — bereinigt.
  Fix für künftig: alten Inhalt zuerst löschen, dann einfügen.
- Schritt 11 (Import-Check): grep traf auf eigenen Beweis-Hinweis im Docstring +
  `__pycache__`-Bytecode. Fix: Docstring-Zeile entfernt, `--include="*.py"` als
  Standard für alle künftigen grep-Checks.

## Comprehension Gate

Dependency Inversion: Antwort lückenhaft. Konzept "Abhängigkeitspfeil invertiert,
Service kennt nur die Abstraktion" noch nicht sicher sitzen. Tag 23 bekommt
Verständnis-Block am Anfang.

## Offene Punkte / nächster Tag

Tag 23: FastAPI-Wrapper — `/health`, Triage-Endpoint, Request/Response-Schemas
(getrennt von Domain-Typen), globaler Exception-Handler.
Verständnis-Block zu DIP am Anfang.
