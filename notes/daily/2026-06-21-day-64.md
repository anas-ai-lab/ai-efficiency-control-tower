# Tag 64 — Daily Note

## Ziel
Experten-Labels fuer 3 der 4 Golden-Cases setzen (unabhaengiges Urteil vor Kenntnis
der Berechnung), Tests anpassen, ersten echten Eval-Report mit Agreement-Rate
erzeugen (Master-Plan v3.1 Phase E, ADR-0029/0030).

## Was gebaut wurde
- `evals/golden/use_cases.jsonl`: expected_zone gesetzt — golden-001 (LIKELY_WIN),
  golden-002 (LIKELY_WIN), golden-003 (CALCULATED_RISK). golden-004 bleibt null,
  bewusst — Vorfilter-Grenzfall, Zonen-Label dort bedeutungslos (ADR-0029).
- `tests/application/eval/test_loader.py`: `test_golden_cases_have_no_expert_label_yet`
  (veraltete Invariante) ersetzt durch `test_golden_cases_expert_labels_set_tag_64`.
- `tests/application/eval/test_runner.py`: `test_unlabeled_golden_cases_have_none_match`
  ersetzt durch `test_only_golden_004_has_none_match`; `test_report_structure_for_unlabeled_cases`
  ersetzt durch `test_report_structure_for_golden_cases` (labeled_cases=3). Beide
  pruefen nur DASS ein Vergleich stattfindet, nicht OB er uebereinstimmt.
- `scripts/run_golden_eval.py` (neu): laedt Golden-Cases, ruft run_eval()+write_report()
  auf, druckt case-weise predicted/expected/is_match. Kein CLI-Flag (ADR-0030).
- `evals/golden/report.json` (neu, generiert).

## Tests / Verifikation
- `uv run pytest`: gruen. `uv run mypy src/`: gruen.
- Script-Lauf: Agreement 1/3. golden-002 stimmt, golden-001 + golden-003 weichen ab.

## Abweichungen / Funde
- Methodik-Fund: Claude beschrieb golden-003 beim Elizitieren falsch (3 von 3
  Handlungsdruck-Flags statt 2 von 3). Anas mit korrigierten Daten erneut gefragt —
  Urteil (CALCULATED_RISK) unveraendert. Befund: die Art, wie ein Fall einem
  menschlichen Rater praesentiert wird, ist selbst eine Fehlerquelle im
  Experten-Abgleich (Eval-Methodik) — kein reiner Bug, methodisch relevant.
- Beide Mismatches liefern nur "uneinig", keine Ursache. EvalCaseResult hat keine
  Score-Breakdown-Komponenten — moegliche spaetere Erweiterung, nicht heute.

## Comprehension Gate

**Kontext:** Heute hast du drei Faelle eingestuft, bevor du wusstest, was die
Berechnung sagen wuerde — bei einem davon zweimal, weil eine falsche Tatsache
genannt wurde. Trotz Korrektur kam dasselbe Urteil heraus. Ergebnis: 1 von 3
Faellen stimmt mit der Maschine ueberein.

**Frage:** Was sagt es ueber den Wert dieses Tests, dass du bei golden-003 auch
nach korrigierten Fakten beim selben Urteil geblieben bist?

**Antwort:** Nicht explizit beantwortet — Anas ging direkt zum Abschluss ueber
(explizite Anweisung "mach jetzt daily und learning"). Keine Wiederholung der Frage.

## Dateien geaendert
- evals/golden/use_cases.jsonl (geaendert)
- tests/application/eval/test_loader.py (geaendert)
- tests/application/eval/test_runner.py (geaendert)
- scripts/run_golden_eval.py (neu)
- evals/golden/report.json (neu, generiert)

## Naechster Schritt
Optional: Score-Breakdown pro Case sichtbar machen, um Mismatches diagnostizieren
zu koennen. Sonst: CLI-Entrypoint mit --provider-Flag (ADR-0030), sobald LLM-Pfade
Teil des Evals werden. Phase wird zu Sessionstart neu aus dem Repo bestimmt.

**Empfohlenes Modell:** Sonnet · Effort: niedrig.
