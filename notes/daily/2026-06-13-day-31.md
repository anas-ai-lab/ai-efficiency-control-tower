# Tag 31 — Use-Case-Schärfung: Prompt-Loader + LLM-Wiring

**Phase:** C (LLM-Integration, Mock-First)

## Was wurde gebaut
- `prompts/sharpen_use_case/v1/{system,user}.md` — versionierte, generische
  Prompt-Dateien (Data/Instruction-Trennung via `<<<DATA>>>`-Delimiter)
- `aect.application.prompts.load_prompt()` — Pfadauflösung analog `load_roi_config()`
- `SharpenedUseCase` (application/models.py) — Original-Felder + geschärfter Text
  + Prompt-Version
- `TriageService.sharpen_case()` — neue async Methode, `LLMPort` als Pflicht-
  Konstruktorparameter (Constructor-DI, kein Default → keine Adapter-Kopplung)
- `get_llm_adapter()` (dependencies.py) — liefert `MockLLMAdapter`, einzige
  Austauschstelle für einen späteren Azure-Adapter
- `POST /cases/{case_id}/sharpen` (routes/cases.py) — Auth, 10/min Rate-Limit, 404
  bei unbekannter Case-ID
- ADR-0006: Prompt-Loader, Versionierung, Schärfungs-Workflow inkl. offener Punkte
  (Output-Validation, LLM-Red-Team-Tests — verschoben auf Azure-Adapter-Tag)

## Tests
227 passed, 99% Coverage. Alle 5 `TriageService(...)`-Instanziierungen (1 produktiv,
4 Tests) auf `llm`-Parameter angepasst.

## Stolpersteine
- Schritt 8 zunächst nur teilweise angewendet (`test_idempotency.py`,
  `test_rate_limiting.py` initial übersehen) → 4 Tests failed mit
  `TypeError: missing 1 required positional argument: 'llm'`. Nachgezogen.
- Import-Reihenfolge in `test_service.py` nach Schritt 6 unsortiert (RUF I001) —
  per ruff-Fix korrigiert.
- ADR-Nummerierung: zwei koexistierende Serien (`000X` und `ADR-00X`) entdeckt,
  in session-protocol v3.1 dokumentiert. Kein Funktionsverlust, aber Phase-F-
  Review-Punkt (möglicher Duplikat 0002 vs. ADR-004, beide "Hexagonale Architektur").

## Offene Punkte
- Output-Validation der LLM-Antwort (striktes Pydantic-Schema) noch nicht
  umgesetzt — `sharpened_text` ist `str`. Folgt mit Azure-Adapter (ADR-0006).
- Phase-F-Review: ADR-Doppelserie, möglicher Duplikat 0002/ADR-004.

## Nächster Tag
Azure-OpenAI-Adapter (Phase C, §4-Checkpoint: `.env`-Credentials prüfen) oder
Output-Validation/strukturierte LLM-Antwort.
**Empfohlenes Modell:** Sonnet 4.6 · Effort: Medium
