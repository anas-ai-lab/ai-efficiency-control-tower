# Tag 11 — 2026-05-08

## Was gemacht
- pre-commit als Dev-Dependency installiert (`uv add --dev pre-commit`)
- `.pre-commit-config.yaml` erstellt mit 10 Hooks:
  ruff, ruff-format, mypy (local), end-of-file-fixer, trailing-whitespace,
  check-yaml, check-json, check-toml, debug-statements, check-merge-conflict
- `pre-commit install` → Hooks in `.git/hooks/pre-commit` registriert
- `pre-commit autoupdate` → deprecated stage-name Warning für pre-commit-hooks gefixed
- Erster `--all-files` Run: ruff-format reformatierte 2 Python-Files,
  end-of-file-fixer fixte 15 Markdown/Config-Dateien, trailing-whitespace fixte 6 Dateien
- Nach zweitem Run alle Hooks grün
- Commit-Stolperfalle: Hooks modifizierten Dateien während Commit → unstaged →
  mit `git add -A` und erneutem Commit gelöst
- Alle Änderungen gepusht

## Was gelernt
- `no files to check` = korrekt bei nicht-gestagten Dateien — kein Bug
- mypy als `local` Hook nötig: mirrors-mypy kennt Projekt-venv nicht → falsche Import-Fehler
- Hooks fixen Dateien beim ersten Run → immer zweimal laufen lassen bis grün
- Bei Commit: wenn Hook Datei modifiziert, bricht Commit ab → `git add -A` + erneut committen

## Sichtbarer Output
- `.pre-commit-config.yaml` (committed)
- Pre-commit in `.git/hooks/pre-commit` aktiv
- Gesamtes Repo: trailing whitespace + end-of-file bereinigt
- `pyproject.toml` + `setup_notes.md` aktualisiert
