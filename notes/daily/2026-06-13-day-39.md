# Tag 39 — Daily Note

## Ziel
`propose_solution()` hatte einen ungetesteten Pfad: das LLM bekommt Werkzeuge
angeboten, fordert aber keines an und antwortet direkt. Heute kam der
passende Test dazu.

## Was gebaut wurde
- Neue Fake-LLM-Klasse `_NoToolCallLLMAdapter` in `tests/application/test_service.py`:
  antwortet immer direkt, ignoriert angebotene `tools`.
- Neuer Test `TestTriageServiceProposeSolutionNoToolCall::test_direct_response_without_tool_call`:
  prüft, dass `propose_solution()` in diesem Fall eine direkte Antwort liefert
  und genau ein Cost-Log entsteht (statt zwei beim Tool-Call-Pfad).
- Ergebnis: `service.py` jetzt 100% Branch-Coverage (vorher 99%, Lücke
  `230->261`). Alle drei möglichen Reaktionen des LLM auf das Tool-Angebot
  sind jetzt getestet: kein Call / bekanntes Tool / unbekanntes Tool.

## Comprehension Gate
**Frage:** Wenn die KI direkt antwortet, ohne ein Werkzeug anzufordern — wie
viele Kosten-Log-Einträge entstehen dabei, und warum ist das anders als beim
Werkzeug-Pfad?

**Antwort (User):** Dann entsteht nur ein Kostenlog, weil es kein Hin und Her
gibt.

**Status:** ✅ korrekt beantwortet, kein Ergänzungsbedarf für Tag 40.

## Dateien geändert
- `tests/application/test_service.py` (+1 Fake-Adapter, +1 Test-Klasse)

## Tests
275 Tests grün (vorher 274). `uv run mypy src/` ohne Fehler. Coverage
`service.py`: 100%.

## Nächster Schritt
Tag 40 — vorläufig Output-Validation für LLM-Antworten (ADR-0006, offener
Punkt aus Phase C) oder Report-Renderer; wird zu Sessionstart aus dem
aktuellen Repo-Stand neu verifiziert.

**Empfohlenes Modell:** Sonnet · Effort: medium
