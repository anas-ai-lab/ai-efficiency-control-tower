# Tag 35 — DI-Wiring ResilientLLMAdapter

## Ziel
`get_llm_adapter()` liefert ResilientLLMAdapter(MockLLMAdapter()) statt
MockLLMAdapter() direkt. Circuit-Breaker (tenacity Retry/Backoff/Timeout,
Tag 34) ist damit fuer jeden produktiven LLM-Call im DI-Graph aktiv.

## Geaendert
- src/aect/adapters/api/dependencies.py
  - Import ResilientLLMAdapter ergaenzt
  - get_llm_adapter() wrapt MockLLMAdapter mit ResilientLLMAdapter
  - Docstrings get_llm_adapter() + get_triage_service() aktualisiert
    (DI-Graph-Realitaet statt veralteter "immer MockLLMAdapter"-Aussage)
- tests/adapters/api/test_dependencies.py (neu)
  - test_get_llm_adapter_returns_resilient_wrapped_adapter:
    isinstance-Check + echter complete()-Durchlauf gegen Mock

## Status
258/258 Tests gruen. Coverage unveraendert ~99% (dependencies.py 86/149,
resilient.py 80, routing.py 117, scoring.py 54 bewusst offen -- nicht
heutiger Scope).

## Security-Bezug
aect-security-checklist v2.1 Phase C "Circuit Breaker (tenacity): Retry +
Backoff + harter Timeout" -- jetzt verdrahtet, nicht mehr nur isoliert
getestet (test_resilient.py).

## Bewusst nicht angefasst
- Retry-Exception-Typen (TimeoutError, ConnectionError) -- Platzhalter,
  Erweiterung erst mit Azure-OpenAI-Adapter (dokumentiert in resilient.py).
- Coverage-Luecken dependencies.py 86/149.

## Comprehension Gate
Antwort 1 (warum Bewertungslogik unangetastet): unvollstaendig --
"kein LLM" genannt, Port/Decorator-Kern fehlt. Ergaenzung Tag 36.
Antwort 2: Frage verstiess gegen §3-Format (verlangte Code-Kontext,
den der User nicht hat) -- Lehre fuer kuenftige Gates: Fragen muessen
aus dem 2-3-Saetze-Kontext beantwortbar sein, nie aus Code/Memory.

## Naechster Tag
Stack-Mapping / Loesungsvorschlag (Phase C, Mock-First).
cat-Pflicht: application/prompts.py, application/service.py,
application/models.py, adapters/api/routes/triage.py.
Empfohlenes Modell: Sonnet · Effort: medium.
