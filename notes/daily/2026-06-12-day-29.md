# Tag 29 — Phase-B-Abschluss: ADRs + Quality Gate

**Datum:** 2026-06-12
**Phase:** B (Hexagonal, API-Layer & Persistenz) — heute formal abgeschlossen

## Ziel
Zwei offene ADR-Luecken aus Master-Plan v3.1 Phase B schliessen (Auth,
SQLite-Persistenz) und das Quality Gate B->C durchfuehren.

## Was wurde gebaut
- `docs/adr/ADR-006-api-key-auth.md`: dokumentiert die seit Tag 24
  bestehende API-Key-Authentifizierung (`X-API-Key`, `require_api_key()`),
  Alternativen (JWT/RBAC, OAuth2) und warum sie fuer ein Single-User-System
  verworfen wurden.
- `docs/adr/ADR-007-sqlite-persistence.md`: dokumentiert die seit Tag 27
  bestehende `SQLiteRepository`-Implementierung, Alternativen (PostgreSQL,
  In-Memory, ORM) und Trade-offs (kein Migrations-Tooling, Pro-Request-
  Connections).

## Quality Gate Phase B -> C
```bash
uv run uvicorn aect.adapters.api.app:app --reload-dir src
curl -s http://localhost:8000/health
curl -s -i http://localhost:8000/cases
```
- `/health` -> 200, `{"status":"ok","version":"0.1.0"}`, kein Auth.
- `/cases` ohne Key -> 401, `{"detail":"Invalid or missing API key"}`,
  Header `www-authenticate: X-API-Key`, kein Stack-Trace.
- `uv run pytest -q` -> 214 Tests gruen (vorab bestaetigt).

**Ergebnis:** Gate erfuellt. **Phase B ist abgeschlossen.**

## Comprehension-Gate Tag 28 (Nachtrag)
Frage zu Idempotency-Keys teilweise beantwortet -- Doppelspeicherung korrekt
erkannt, "warum reicht In-Request-Pruefung nicht" nachgetragen: ein Retry ist
ein komplett neuer Request ohne Kenntnis vom vorherigen -- deshalb braucht es
einen Store mit Zustand ueber Request-Grenzen hinweg (`IdempotencyStorePort`).

## Was bewusst nicht angefasst wurde
- `docs/adr/0001-0003` (altes Nummerierungsschema) -- Inkonsistenz mit
  `ADR-00X`-Schema notiert, Bereinigung gehoert in `phase-b-review.md`.
- Kein Code geaendert -- nur Dokumentation (ADRs).

## Offene Punkte fuer naechste Sessions
- `docs/reviews/phase-b-review.md` schreiben (Coverage-Stand, was anders
  designt wuerde, offene technische Schulden inkl. ADR-Nummerierung).
- Danach Start Phase C: `LLMPort`-Protokoll + Mock-Adapter.

## Naechster Tag
Tag 30 — Start Phase C: `LLMPort`-Protokoll + deterministischer
Mock-Adapter (mock-first, Master-Plan v3.1 Phase C).
**Empfohlenes Modell:** Sonnet · Effort: medium.
