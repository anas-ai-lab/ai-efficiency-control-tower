# Tag 60 — Daily Note

## Ziel
Cross-Encoder-Reranking als zweite Stufe nach dem Hybrid-Retrieval einziehen — letzter offener Hybrid-Begleitpunkt aus Tag 59 (Master-Plan v3.1, Phase D).

## Was gebaut wurde
- `src/aect/adapters/rag/reranker.py` (neu): `CrossEncoderModel`-Protocol (struktureller Typ, analog `EncoderModel` in `embedder.py`), `CrossEncoderReranker` — implementiert `RetrieverPort` via strukturellem Subtyping, wrappt einen beliebigen inneren `RetrieverPort` (im echten Pfad: `HybridRetriever`). `candidate_pool=10` (Default, analog `HybridRetriever.candidate_pool`): innerer Retriever wird mit `top_k=candidate_pool` abgefragt, nicht mit finalem `top_k`. `RetrievedChunk.score` wird durch den Cross-Encoder-Score ersetzt (vertragskonform laut `ports/retriever.py`: score ist nur für Reihenfolge/Anzeige). Blockierende `.predict()`-Inferenz läuft in `asyncio.to_thread`, analog `SentenceTransformerEmbedder.embed()`.
- `src/aect/adapters/api/dependencies.py`: neue Konstante `_CROSS_ENCODER_MODEL_NAME = "cross-encoder/ms-marco-MiniLM-L-6-v2"`, neuer gecachter Helper `_get_cross_encoder_model()` (lokaler Import, lru_cache, analog `_get_local_embedding_model`/`_get_bm25_index`). `get_retriever_port()` liefert im realen Pfad (`AECT_CHROMA_HOST` gesetzt) jetzt `CrossEncoderReranker(HybridRetriever(ChromaRetriever, BM25Retriever), model)` statt nacktem `HybridRetriever`. Mock-Pfad unverändert.
- `src/aect/adapters/rag/__init__.py`: Export um `CrossEncoderReranker` erweitert.
- `docs/adr/0028-cross-encoder-reranking.md` (neu): Wrapping-statt-Erweiterung-Entscheidung, Modellwahl (`ms-marco-MiniLM-L-6-v2`, kein größeres Modell, kein LLM-basiertes Reranking) gegen Alternativen begründet, candidate_pool-Übernahme, Score-Ersetzung, Code-Konstante statt Settings-Feld begründet.

## Tests (neu/geändert)
- `tests/adapters/rag/test_reranker.py` (neu, 7 Tests): Umsortierung nach Cross-Encoder-Score, top_k nach Reranking, candidate_pool statt top_k an inneren Retriever weitergereicht, leere Ergebnisse, Score-Ersetzung, Text-/Metadaten-Erhalt, Query-Dokument-Paarbildung.
- `tests/adapters/api/test_dependencies.py`: alte `test_get_retriever_port_wires_hybrid_retriever_with_host_set` ersetzt durch `test_get_retriever_port_wires_cross_encoder_reranker_with_host_set` — prüft jetzt die dreistufige Verdrahtung (`CrossEncoderReranker._inner` ist `HybridRetriever`), `_get_cross_encoder_model` gefaked (kein realer HF-Hub-Download im Testlauf mehr).

Bestätigung: User meldet vollen Testlauf grün und committet.

## Abweichungen / Funde
Zwischenzeitlich lief `tests/adapters/api/test_dependencies.py` mit dem alten Testnamen gegen die neue `dependencies.py` und schlug erwartungsgemäß fehl (`isinstance(retriever, HybridRetriever)` falsch, da jetzt `CrossEncoderReranker`) — Ursache: Schritt 5 (Testersatz) war zu diesem Zeitpunkt noch nicht durchgeführt, kein Bug. Nebenbefund dabei: ohne den gefakten `_get_cross_encoder_model`-Mock lädt der Testlauf real ein Modell von HuggingFace (sichtbar an der HF-Hub-Warnung im Log) — kein Kostenpunkt, aber ein Grund mehr, warum der Mock in Schritt 5 notwendig war, nicht optional.

## Comprehension Gate

**Kontext:** Bisher kam aus der zweistufigen Hybrid-Suche bereits eine sinnvoll sortierte Trefferliste — aber beide Suchverfahren bewerten Frage und Textstelle getrennt voneinander, ohne sie direkt nebeneinanderzulegen. Heute kam ein dritter Schritt dazu, der genau das tut: Frage und jeden Kandidaten-Text gemeinsam betrachten und neu, präziser sortieren — bevor das System die besten Treffer weiterverwendet.

**Frage 1:** Warum lässt man diesen dritten, genaueren Schritt nicht gleich über die komplette Wissensbasis laufen, statt nur über die wenigen Kandidaten aus Schritt eins und zwei?

**Frage 2:** Was würde sich am Ergebnis ändern, wenn dieser dritte Schritt nur die obersten 2 Kandidaten statt 10 betrachten würde?

**Status: OFFEN — noch nicht beantwortet.** Tag 61 ist damit blockiert (§3: "Keine Antwort = kein nächster Guide").

## Dateien geändert
- `src/aect/adapters/rag/reranker.py` (neu)
- `src/aect/adapters/api/dependencies.py` (geändert)
- `src/aect/adapters/rag/__init__.py` (geändert)
- `tests/adapters/rag/test_reranker.py` (neu)
- `tests/adapters/api/test_dependencies.py` (geändert)
- `docs/adr/0028-cross-encoder-reranking.md` (neu)

## Nächster Schritt
1. Comprehension Gate Tag 60 beantworten (Blocker für Tag 61).
2. Phase-D-Gate-Check (session-protocol v3 §2, D→E): 3 Test-Queries mit Citations gegen den vollen Hybrid+Reranking-Pfad — offen seit Tag 57.
3. Fallen-Katalog (§6) um macOS-sed-Regex-Punkt ergänzen — offen seit Tag 56.

**Empfohlenes Modell:** Sonnet · Effort: mittel.
