# Tag 57 — Daily Note

## Ziel
ChromaRetriever in die DI verdrahten (Settings-gesteuerter Mock-vs-Real-Schalter, analog get_llm_adapter()). Echte Wissensbasis seeden. Verdrahtung per Live-Test gegen drei Queries mit Citations beweisen — automatisierter Phase-D-Gate-Check (session-protocol v3 §2).

## Was gebaut wurde
- `src/aect/adapters/api/settings.py`: `chroma_host` (Default `""`), `chroma_port` (Default `8001`) ergänzt.
- `.env.example`: neuer, auskommentierter `AECT_CHROMA_HOST`/`AECT_CHROMA_PORT`-Block dokumentiert.
- `src/aect/adapters/api/dependencies.py`: `get_retriever_port()` settings-gesteuert — `chroma_host` leer → `MockRetriever` (unverändert), gesetzt → `ChromaRetriever` gegen die echte Collection `aect-knowledge-base`. Zwei neue `lru_cache`-Hilfsfunktionen `_get_chroma_collection(host, port)` und `_get_local_embedding_model()`: Chroma-Client/Collection-Handle und das sentence-transformers-Modell werden je einmal pro Prozess gebaut, nicht pro Request. `chromadb`/`sentence-transformers`-Imports bewusst lokal in der Funktion (nicht Modulkopf) — der Mock-Pfad und alle normalen Testläufe bleiben torch-/chromadb-frei (Fortsetzung ADR-0016).
- `tests/adapters/api/test_dependencies.py`: 2 neue Tests (`test_get_retriever_port_returns_mock_without_chroma_host`, `test_get_retriever_port_wires_chroma_retriever_with_host_set` — letzterer fakt `_get_chroma_collection`/`_get_local_embedding_model` per `monkeypatch`, kein echter Chroma-/Torch-Aufruf im Standardlauf).
- `scripts/seed_knowledge_base.py` (neu, Ordner `scripts/` neu angelegt): einmaliges, idempotentes Seeding-Skript — liest `knowledge_base/`, bettet über `SentenceTransformerEmbedder` ein, schreibt via `index_knowledge_base()` in die Collection `aect-knowledge-base`.
- `tests/adapters/api/test_dependencies_live.py` (neu): Live-Test (`AECT_RUN_CHROMA_LIVE=1`), prüft `get_retriever_port()` im echten Pfad gegen 3 reale Queries — jede liefert mindestens einen Treffer mit Citation-Metadaten und keine `mock-`-Source-IDs.
- `docs/adr/0025-chroma-retriever-di-wiring.md` (neu): Schalter-Design, lru_cache-Begründung, Seeding-als-eigenes-Skript-Entscheidung dokumentiert.

## Ausführung
- `docker compose up -d` → Container `aect-chromadb` healthy.
- `uv run python scripts/seed_knowledge_base.py` → 5 Chunks aus `knowledge_base/` indiziert (Collection `aect-knowledge-base`).
- Live-Test (`AECT_RUN_CHROMA_LIVE=1 uv run pytest tests/adapters/api/test_dependencies_live.py -v`) grün — 3 reale Queries liefern echte Citations.
- Voller Testlauf: 384 passed, 3 skipped, ~6.3s (kein Laufzeitanstieg ggü. Vortag — bestätigt: kein Torch-/chromadb-Import im Default-Pfad).

## Abweichungen / Funde
- `scripts/`-Ordner existierte noch nicht im Repo — neu angelegt (`mkdir -p scripts`), keine Konvention verletzt, da es die erste Datei dieser Art ist.
- Ein einzelner Testlauf zeigte `test_azure_openai_live.py::test_real_azure_call_costs_under_one_cent` als `TimeoutError` (httpx `ConnectTimeout`) — beim direkten Re-Run grün. Transienter Netzwerk-Hänger gegen den Azure-Endpoint, kein struktureller Fund, kein Zusammenhang mit dem heutigen RAG-Code (Coverage von `dependencies.py`/`settings.py` unverändert sauber in beiden Läufen).

## Comprehension Gate

**Frage 1:** Wenn die Einstellung auf "echt" steht, der Such-Server aber gar nicht gestartet ist — greift das System automatisch wieder auf die Test-Bibliothek zurück, oder gibt es einen Fehler?
**Antwort (User):** "Fehler"
**Status:** Korrekt — kein stiller Fallback auf Mock; ein gesetzter `AECT_CHROMA_HOST` ohne laufenden Container führt zu einem Verbindungsfehler beim ersten echten Retrieval-Call.

**Frage 2:** Lädt das System das Modell zehnmal, wenn zehn Anfragen kurz hintereinander reinkommen, oder einmal?
**Antwort (User):** "10x"
**Status:** Nicht korrekt — präzisiert im Chat. Richtig: **einmal pro Prozess**. Der `lru_cache` auf `_get_local_embedding_model()` hält die geladene Modell-Instanz über alle Requests hinweg fest; nur der allererste Aufruf lädt, alle folgenden nutzen das gecachte Objekt.

## Dateien geändert
- `src/aect/adapters/api/settings.py` (geändert)
- `.env.example` (geändert)
- `src/aect/adapters/api/dependencies.py` (geändert)
- `tests/adapters/api/test_dependencies.py` (geändert)
- `scripts/seed_knowledge_base.py` (neu)
- `tests/adapters/api/test_dependencies_live.py` (neu)
- `docs/adr/0025-chroma-retriever-di-wiring.md` (neu)

## Tests
`uv run pytest tests/adapters/api/test_dependencies.py -v` — 4 passed.
`AECT_RUN_CHROMA_LIVE=1 uv run pytest tests/adapters/api/test_dependencies_live.py -v` — 1 passed.
`uv run pytest -q` — 384 passed, 3 skipped.
Final-Commit-Block (pre-commit/pytest/mypy/bandit/pip-audit) laut Guide ausgeführt, gepusht.

## Nächster Schritt
1. Persistenz von `compliance_hints` auf `SubmittedCase` + Einbau in `/report` — gleiches Muster wie `sharpen_case`/`propose_solution` vor Tag 42 (ADR-0012).
2. Hybrid Search (BM25 + Vektor + RRF) — aktuell reines Vektor-Retrieval, offener Phase-D-Scope-Punkt.
3. Fallen-Katalog (§6) um den macOS-`sed`-Regex-Punkt ergänzen (offen seit Tag 56).

**Empfohlenes Modell:** Sonnet · Effort: mittel.
