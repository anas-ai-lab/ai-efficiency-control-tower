# Tag 37 — Function-Calling-Vorbereitung (Phase C)

**Datum:** 2026-06-13
**Phase:** C (LLM-Integration, Mock-First)

## Was wurde gebaut

- `LLMPort.complete()` um optionalen `tools`-Parameter erweitert
  (`ToolDefinition`, `ToolCall`, `role="tool"`, `tool_call_id`,
  `tool_calls`). Rueckwaertskompatibel -- Default `None`.
- `MockLLMAdapter`: simuliert genau einen Tool-Call, wenn `tools`
  angeboten werden und die Historie noch keine `role="tool"`-Antwort
  enthaelt; sonst Echo-Verhalten wie bisher.
- `ResilientLLMAdapter`: reiner Passthrough fuer `tools`, Retry/Timeout
  unveraendert.
- Neues Modul `application/tools.py`: `TOOL_DEFINITIONS`,
  `lookup_stack_options()`, `dispatch_tool_call()`, `UnknownToolError`.
  Erstes Tool: `lookup_stack_options` (parameterlos).
- Neue Config `config/stack_options.toml`: 5 Plattform-Optionen
  (Open WebUI, Copilot Studio, Foundry, SAP BTP, Andere) -- IP-Trennung,
  Beschreibungen sind Tag-37-Platzhalter ohne RAG-Beleg.
- ADR-0008: Function-Calling-Tool-Layer, inkl. offenem Punkt fuer Tag 38
  (Loop-Architektur: Service-inline vs. eigener Adapter).

## Tests
- +8 neue Tests (2 Mock, 1 Resilient, 5 Tools). Alle bestehenden Tests
  unveraendert gruen.
- `uv run pytest -q`: 270 passed (262 + 8), Coverage >= 99%.

## Was bewusst NICHT gemacht wurde
- Keine Verdrahtung in `TriageService.propose_solution()` -- die
  Function-Calling-Loop ist Tag 38.
- Keine RAG-Anbindung fuer `lookup_stack_options` -- Phase D.
- Keine Aenderung an `sharpen_case()`.

## Offene Punkte fuer Tag 38
- Loop-Architektur entscheiden (ADR-0008): inline in `propose_solution()`
  vs. eigener `ToolCallingLLMAdapter`.
- Tool-Fail-Test (Gate-Voraussetzung C->D).
- Comprehension-Gate "Function-Calling-Loop" (session-protocol v3 §3).
- Hinweis im System-Prompt von `propose_solution`, dass Plattform-
  Beschreibungen vorlaeufig/unbelegt sind (bis Phase D).

## Comprehension Gate -- Antworten
1. Code garantiert, dass das Wissen ueber verfuegbare Plattformen aktuell
   und vollstaendig ist -- unabhaengig von Trainingsdaten-Stand.
2. (Praezisiert) Der Code lehnt unbekannte Tool-Namen ab
   (`UnknownToolError`) -- eine Rueckmeldung *an das LLM* ist Teil der
   Tag-38-Loop, heute noch nicht verdrahtet.
