# Tag 12 — 2026-06-05

## Was heute fertig wurde

- `.github/workflows/ci.yml` — 3 Jobs: quality (ruff, mypy, pytest+cov),
  security (bandit MEDIUM+, pip-audit), secrets (gitleaks v3)
- Alle Actions SHA-gepinnt (checkout v6.0.2, setup-uv v8.1.0, gitleaks v3)
- `bandit[toml]>=1.8` und `pip-audit>=2.10.0` als Dev-Dependencies
- `[tool.bandit]` Config in `pyproject.toml`
- CVE-Fix: `idna` 3.13 → 3.15 (CVE-2026-45409), von pip-audit erkannt
- README: CI-Badge eingefügt
- Erster CI-Run auf GitHub ausgeführt

## Entscheidungen

- `actions/checkout@v6` statt v4: GitHub Node-24-Transition am 2. Juni 2026
- `gitleaks/gitleaks-action@v3`: v2 läuft nicht mehr auf Node 24
- SHA-Pins über Tags: Supply-Chain-Schutz, nicht optional für ein Projekt
  das später PII verarbeitet
- Branch Protection auf Free Plan nicht durchsetzbar auf private Repos —
  dokumentiert, kein Blocker für Solo-Build

## Offen / Follow-up Tag 13

- `mypy.ini`: Parse-Fehler in Zeile 16 (`']\n'`) — mypy läuft durch aber
  ignoriert die ini teilweise. Strict-Settings könnten nicht greifen.
  Fix: Datei inspizieren, Syntax-Fehler beheben.
- Branch Protection Status-Checks nachträglich eintragen sobald CI
  mindestens einmal grün war (jetzt möglich)

## Zahlen

- 5 Files geändert, 498 Insertions im ersten Commit
- 4 Actions SHA-gepinnt
- 1 CVE gefunden und gefixt (idna, CVE-2026-45409)
- CI-Laufzeit: ~17s (erster Run)
