# Tag 30 — LLM-Port + Mock-Adapter

**Datum:** 2026-06-12
**Phase:** C (erster Tag) — Gate B→C erfüllt (/health antwortet 200 mit
{"status":"ok","version":"0.1.0"})

## Was gebaut wurde

- `src/aect/application/ports/llm.py`: `LLMPort` (Protocol), `LLMMessage`
  (role: system/user/assistant + content), `LLMResponse`. Folgt exakt dem
  Pattern von `ClockPort`/`IdGeneratorPort`.
- `src/aect/adapters/in_memory/llm.py`: `MockLLMAdapter` — implementiert
  `LLMPort`, gibt deterministisch `"[mock-response] <letzter User-Text>"`
  zurück. Kein echter LLM-Call, kein Netzwerk, keine Kosten.
- 4 neue Tests in `tests/adapters/in_memory/test_llm.py`.
- `docs/adr/0005-llm-port-messages-api.md`: ADR-Stub zur Messages-API-
  Trennung (System/User getrennt, kein String-Concat).

## Ergebnis

- 218 Tests grün (vorher 214 + 4 neue), mypy strict clean, ruff clean.
- Commit gepusht: `feat(application): LLM port and deterministic mock adapter`

## Security / IP

- Keine firmenspezifischen Werte berührt — vollständig generischer Code.
- Messages-API-Trennung (System/User) ist Vorbereitung für
  aect-security-checklist v2.1 Phase C (kein String-Concat vor LLM-Call).

## Comprehension Gate

Bestanden. Kernaussage: Mock-Adapter gibt immer dieselbe, vorhersagbare
Antwort → Tests können prüfen "kommt das Erwartete an" UND laufen offline,
kostenlos, reproduzierbar — Grundlage für die gesamte Phase-C-Test-Suite
(Schärfung, Lösungsvorschlag, Report bauen später darauf auf).

## Nächster Tag

Prompt-Loader + Versionierung (`prompts/`), erste Verdrahtung des
`MockLLMAdapter` über den `LLMPort` in einen kleinen Service-Ausschnitt.
Empfohlenes Modell: Sonnet · Effort: medium.
