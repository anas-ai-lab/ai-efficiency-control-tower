# Tag 23 — 2026-06-10

## Was gebaut wurde
- FastAPI App-Factory (`src/aect/adapters/api/app.py`) mit CORS, debug=False
- `/health`-Endpoint (`src/aect/adapters/api/routes/health.py`)
- Dependency-Provider (`src/aect/adapters/api/dependencies.py`)
- 4 async Tests via httpx.AsyncClient + ASGITransport

## Neue Dateien
- src/aect/adapters/api/__init__.py
- src/aect/adapters/api/app.py
- src/aect/adapters/api/dependencies.py
- src/aect/adapters/api/routes/__init__.py
- src/aect/adapters/api/routes/health.py
- tests/adapters/__init__.py
- tests/adapters/api/__init__.py
- tests/adapters/api/test_health.py

## Entscheidungen
- create_app() statt globaler app-Instanz in Tests: jeder Test bekommt
  isolierte App ohne geteilten Middleware-State.
- CORS allow_origins=[]: sicherer Default; Phase F befüllt per Env-Variable.
- debug=False hart kodiert: kein Stack-Trace an Client (OWASP LLM02).

## Probleme
- venv zweimal korrupt (py-Modul fehlend, _hypothesis_pytestplugin fehlend)
  nach uv add. Fix jeweils: rm -rf .venv && uv sync.
- asyncio_mode warning: pytest-asyncio fehlte (lag in test-Group,
  nicht in dev). Fix: uv add --dev pytest-asyncio.

## Stand
164 Tests grün. API antwortet auf /health.
Nächster Tag: Triage-Endpoint, API-Key-Auth, Rate-Limiting, Exception-Handler.
