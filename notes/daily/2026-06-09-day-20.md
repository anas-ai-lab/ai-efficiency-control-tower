# Tag 20 — 09. Juni 2026

## Was gebaut wurde
`domain/pipeline.py` mit `evaluate_use_case()` — die Orchestrierungsfunktion
die alle Phase-A-Regelschichten in einem deterministischen Durchlauf verbindet.
Dazu `tests/domain/test_pipeline.py` mit 10 parametrisierten Snapshot-Cases.

Phase A Gate bestanden: 148 Tests grün, 98% Coverage domain.

## Ausführungsreihenfolge (kritische Erkenntnis)

ROI → Vorfilter → Routing → Feasibility → [Scoring + Zone wenn Filter ok]

ROI muss als erstes laufen, weil `apply_prefilter()` den berechneten
`theoretical_potential_eur` und `net_expected_benefit_eur` aus dem ROI-Ergebnis
braucht. Hätte ich `apply_prefilter()` zuerst geschrieben, wäre die Architektur
falsch gewesen.

## Was wo hakt
`FeasibilityChecker.check()` nimmt keine `UseCaseInput` — es braucht
Rohparameter (Strings, Decimal, int). Das hat mehrere Fehlerschleifen
produziert. Kernregel verletzt: Signaturen zuerst lesen, dann schreiben.

## Weitere Fixes heute
- `mypy.ini` war im TOML-Format geschrieben (`[[mypy.overrides]]`) statt
  im korrekten INI-Format — mypy konnte die Datei nicht parsen
- `__all__` in `__init__.py` nicht alphabetisch sortiert — ruff RUF022
- `FrozenInstanceError`-Test: `object.__setattr__()` umgeht den C-Level-Schutz,
  direktes Assignment (`result.field = x`) wirft korrekt
- Venv-Duplikat `aect-0.1.0 2.dist-info` durch macOS-Finder-Verhalten

## Zahlen
- 148 Tests grün (war: 120 vor Tag 20)
- 98% Coverage domain
- 5 Files geändert, 434 Insertions
- Commit: df2af2d

## Status Phase A
✅ Abgeschlossen. Nächstes: Tag 21 — ADRs + Phase-A-Review-Doc.
