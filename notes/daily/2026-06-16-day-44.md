# Tag 44 — Daily Note

## Ziel
Phase C formal abschliessen: Budget-Sentinel-Check, docs/reviews/phase-c-review.md
mit Midpoint-Reflexion, C->D-Gate-Check ausgefuehrt und dokumentiert.

## Was gebaut wurde
- Env-Check: kein Azure-Account vorhanden, .env leer -> Variante B (Mock-Only)
  bestaetigt (session-protocol v3 SS4).
- `tests/adapters/llm/test_azure_openai_live.py` (neu): Budget-Sentinel-Live-Test,
  skipt automatisch ohne AECT_AZURE_OPENAI_*-Credentials. Bewusst die rohe
  AzureOpenAIAdapter-Instanz (kein ResilientLLMAdapter), um Token-Verbrauch
  durch Retries nicht unnoetig zu vervielfachen.
- C->D-Gate-Check (`uv run pytest tests/adapters/llm/ -q`): Mock-Tests gruen,
  Live-Test skipped (erwartet, da kein Azure-Account).
- `docs/reviews/phase-c-review.md` (neu): Artefakt-Liste Phase C, Design-
  Reflexion (u. a. stale Docstring in resilient.py gefunden -- ADR-0007-
  Punkt ist durch den Azure-Adapter laengst erledigt, nur nicht im Docstring
  nachgezogen), offene technische Schulden, Vertrauens-Werte, Midpoint-
  Reflexion (Antworten ausstehend -- siehe naechster Schritt).

IP-Trennung geprueft: test_azure_openai_live.py liest Credentials
ausschliesslich aus .env via Settings, keine Werte im Code.

## Gate-Status (wichtig, nicht beschoenigen)
Phase C ist NICHT formal abgeschlossen. Offener Punkt: Budget-Sentinel
(echter Azure-Call < 0,01 EUR) fehlt, weil noch kein Azure-Account existiert.
Das ist laut Master-Plan v3.1 / session-protocol v3 SS2 ein Gate-Kriterium
fuer Phase C, nicht ein Phase-D-Task -- bewusst dokumentiert statt versteckt,
analog zur Limitations-Philosophie aus interne Referenz (entfernt) SS7. Kein RAG-Code
(adapters/rag/), bevor dieser Punkt nachgeholt ist. Der EU-AI-Act-Recheck
(Tag 45, erster Schritt) haengt nicht an Azure und kann unabhaengig davon
laufen.

## Comprehension Gate
**Kontext:** Ueber die letzten Tage haben wir die Verbindung zur KI dreimal
ausgetauscht -- von einer Test-Attrappe ueber eine Variante mit
Wiederholungsversuchen bis hin zur (vorbereiteten) echten Azure-Verbindung --
und an der Bewertungslogik (Regeln, Zonen, ROI) hat sich dabei nichts
geaendert. Einen echten Kostenwert konnten wir heute noch nicht messen, weil
kein Azure-Konto existiert.

**Frage 1:** Welcher Teil des Systems hat sich am "austauschbarsten"
angefuehlt -- die KI-Verbindung oder die Bewertungs-Regeln?
**Antwort (User):** Die KI-Verbindung, weil sie relativ unkompliziert ist und
man so keinen Vendor-Lock-in hat.
**Status:** Kern korrekt. Ergaenzung: austauschbar ist sie nicht weil
"unkompliziert" (drei Schichten: Mock/Resilient/Azure), sondern weil
TriageService nur die LLMPort-Schnittstelle kennt, nie eine konkrete
Implementierung. Vendor-Lock-in-Vermeidung ist die Folge dieser Abstraktion,
nicht ihre Ursache.

**Frage 2:** Wir konnten heute nicht echt messen -- warum wollen wir diese
Kostenmessung vor dem RAG-Aufbau erledigt haben, nicht erst danach?
**Antwort (User):** Damit wir sehen, was es uns kosten wird.
**Status:** Kern korrekt. Ergaenzung: es geht nicht nur um Kenntnis der
Kosten, sondern darum, das Kostenmodell (tiktoken-Schaetzung) gegen die
Realitaet zu pruefen, bevor Phase D zusaetzliche Aufrufe (Embeddings, mehr
LLM-Calls pro Request) addiert. Ein Schaetzfehler soll auffallen, solange er
einen einzelnen Call betrifft -- nicht erst nach hunderten RAG-Aufrufen.

## Dateien geaendert
- `tests/adapters/llm/test_azure_openai_live.py` (neu)
- `docs/reviews/phase-c-review.md` (neu)

## Tests
323 Tests erwartet (322 passed + 1 skipped, Live-Test mangels Azure-Account).
Falls dein `uv run pytest` eine andere Zahl zeigt: die tatsaechliche Zahl gilt,
hier korrigieren.

## Naechster Schritt
Tag 45: zwei unabhaengige Stroenge.
1. Azure-Account + Azure-OpenAI-Resource (EU-Data-Zone: swedencentral/
   westeurope) + Deployment gpt-4o-mini + Budget-Alerts 10/20/28 EUR
   einrichten, dann Budget-Sentinel nachholen (Live-Test aus Tag 44 laeuft
   dann scharf). Schliesst das offene C->D-Gate.
2. SS4-Vorab-Check EU-AI-Act-Stand (Digital-Omnibus-Amtsblatt-Status) --
   unabhaengig von Azure, kann parallel/vorher laufen.
Kein adapters/rag/-Code, bevor Punkt 1 erledigt ist.
Offen: Midpoint-Reflexion in docs/reviews/phase-c-review.md (3 Fragen,
Antworten von Anas noch ausstehend).

**Empfohlenes Modell:** Sonnet · Effort: hoch — Begruendung: EU-AI-Act-
Recheck ist laut Security-Checkliste ein load-bearing Faktencheck (Stand
Mai 2026), Fehler pflanzen sich in die gesamte Compliance-Wissensbasis von
Phase D fort. Azure-Setup selbst ist trivial, faellt aber auf denselben Tag.

## Nachtrag — CI-Fixes (vor Tag-44-Abschluss)

Zwei CI-Failures nach dem urspruenglichen Final Commit aufgetreten und
behoben, beide bewusst nicht in den Tag-44-Build-Scope gezaehlt (reine
CI-/Dependency-Hygiene, kein neuer Produktcode):

- **Bandit B608, seit Commit #64 unbemerkt rot:** Tag-43-"Fix" hatte die
  SQL-Strings nur auf Modulebene verschoben, nicht die `+`-Verkettung
  entfernt -- bandit matcht die AST-Form, nicht den Laufzeitwert. Loesung:
  `_INSERT_SQL`/`_SELECT_BY_ID_SQL`/`_SELECT_ALL_SQL` in `repository.py`
  als reine String-Literale ohne `+`-Operator (Spaltenliste dreifach
  dupliziert statt geteilt). Lokal mit `uv run bandit -c pyproject.toml -r
  src/` verifiziert, bevor gepusht wurde.
- **CVE-2026-54282 / CVE-2026-54283 (Starlette, High, DoS via Formular-
  Limits-Bypass), veroeffentlicht 15. Juni 2026 -- keine Altlast, sondern
  brandneue Disclosure zwischen den CI-Runs:** `uv lock --upgrade-package
  starlette` auf >= 1.3.1, danach `uv sync`. Lokal mit `uv run pip-audit`
  verifiziert.

**Lehre, ab jetzt Standard:** `bandit` und `pip-audit` laufen laut
Security-Checkliste nur in CI, nie in pre-commit. Beide laufen ab jetzt
lokal vor jedem Push mit `src/`- oder Dependency-Aenderungen mit, nicht
erst nach dem Push entdeckt.

Tests nach beiden Fixes: weiterhin 323 (322 passed + 1 skipped),
unveraendert -- kein Test entfernt/hinzugefuegt durch diese zwei Fixes.
