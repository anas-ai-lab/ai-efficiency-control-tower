# Screenshots (Platzhalter)

Referenziert aus dem README (Abschnitt "Control-Tower-Module (v3)").
Noch nicht erfasst -- Platzhalter fuer den v3-Closeout:

- `board.png` -- Portfolio-Board (Nutzen-Machbarkeits-Matrix, `/board`)
- `monitoring.png` -- Monitoring-Zeitleiste im Case-Detail (`/monitoring`)

Erfassung im lokalen Betrieb (uvicorn + `npm run dev`), siehe README Quick Start.
